import 'package:flutter/material.dart';

class BodySecond extends StatelessWidget {
  const BodySecond({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center();
  }
}
